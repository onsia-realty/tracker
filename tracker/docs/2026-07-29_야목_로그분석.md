# 야목역 서희스타힐스 방문자 로그 / 부정클릭 점검 — 2026-07-29

> 조회 명령: `cd D:/claude/부정클릭/tracker && node --env-file=.env scripts/<스크립트>.mjs`
> 사용 스크립트: `yamok-week.mjs`, `yamok-yesterday.mjs`, `yamok-keyword-fp.mjs 7`

## 결론

**부정클릭 이상 징후 없음. 차단 조치할 대상 없음.** 대신 광고 쪽에 두 가지 문제가 드러남.

## 최근 7일 (7/22~7/29)

- 세션 **107건** — PC 48 / 모바일 58 / 태블릿 1
- **차단 0건, 의심 1건**(risk 50). 자동 차단 임계(100) 근처도 안 감
- 평균 체류 4분 12초, 5초 미만 즉시이탈 21%
- 일별: 7/22(11) → 7/23(17) → 7/24(14) → 7/25(16) → 7/26(16) → 7/27(19) → 7/28(13)
- 유입: 직접 61, m.search.naver 10, search.naver 7, .xyz 자체도메인 7, navercorp 내부검수봇 6, bing 2, **ad.search.naver 4**

## 어제(7/28 KST) 9세션

| 시각 | 기기 | 지역 | 체류 | 유입 |
|---|---|---|---|---|
| 00:16 | 모바일 | 성남 | 1분28초 | 직접 |
| 12:42 | 모바일 | 수원 | 3분29초 | 직접 |
| 14:38 | PC | 성남 | 2분46초 | **네이버 광고** (utm_term=야목서희스타힐스, powerlink/pc) |
| 15:53 | PC | 성남 | 0초 | 직접 |
| 15:57 | PC | 성남 | 0초 | 직접 |
| 17:34 | PC | 고양 | 1분15초 | 네이버 자연검색 |
| 20:20 | PC | 인천 서구 | 1분19초 | 직접 |
| 20:34 | 모바일 | 안양 | 1분56초 | 직접 |

중복 IP는 `119.69.242.137` 2회뿐이며 같은 세션 연속 — 정상.
**부정클릭 패턴(단시간 동일 fp 연타, 무작위 IP 폭주) 관측되지 않음.**

## 발견된 문제

### 1. 광고 유입이 사실상 멈춤 🚨
7일간 `ad.search.naver.com` 유입 **4건**, 그중 3건이 체류 0초. 어제 광고 유입은 1건.
세션 107건 중 61건이 "직접" → 광고 예산 소진 / 노출 중단 의심.
**→ 네이버 광고관리자에서 예산·노출 상태 확인 필요 (아직 미확인).**

### 2. 키워드 추적 불가
네이버 레퍼러는 검색어를 안 실어줌. 7일간 UTM 붙은 세션 **1건**(`term=야목서희스타힐스`).
어떤 키워드가 예산을 먹는지 알 수 없는 상태.
**→ 해법: 광고관리 > 도구 > 프리미엄 로그분석 활성화** (네이버가 `n_query`/`n_keyword`를 URL에 자동 부착. 키워드별 URL 수정 불필요).
자세한 건 [KEYWORD_TOOL_PLAN.md](./KEYWORD_TOOL_PLAN.md) Phase 6-1.

## 의심 세션 1건 (차단 안 함)

`2026-07-27 03:41 KST` / 모바일 / `106.101.75.219` (서울 동작구) / m.search.naver 유입 / 체류 36초 / risk 50.
106.101.x는 통신사 NAT 공용대역이라 차단해도 무의미하고, 36초 체류 + 전화상담 CTA 클릭까지 있음.
**정상 고객으로 판단 — 차단 비추천.**

## 참고: 블랙키위 연관키워드의 정체

블랙키위는 자체 알고리즘이 아니라 **네이버 데이터 래퍼**다.

| 화면 요소 | 실제 출처 |
|---|---|
| 연관 키워드 200개 | 네이버 검색광고 API `RelKwdStat`(키워드도구) |
| 월간 검색량 | 같은 API의 `monthlyPcQcCnt + monthlyMobileQcCnt` |
| 블로그 누적 발행량 | 네이버 블로그 검색 결과 총건수 |
| 철자 유사도 | 블랙키위 자체 계산(문자열 편집거리) |
| 주제 클러스터 | 키워드 토큰 분해 후 공통어 그룹핑 |

네이버가 말하는 "연관"은 **같은 세션에서 함께 검색/클릭된 키워드**(행동 로그 기반)이지 의미 분석이 아니다.
→ 검색광고 계정만 있으면 키워드도구에서 같은 데이터를 무료로 볼 수 있고, API로도 받을 수 있다. 이게 이번 툴 제작의 출발점.

### CPC 중복과금 오해 정리
"야목역 서희스타힐스 그랜드힐"(1,030회)과 "야목역 서희스타힐스 분양가"(210회)를 둘 다 등록해도 **중복 과금 없음.**
검색어는 한 번에 하나이고, 매칭된 키워드 1개에만 과금된다. 단 **확장검색이 켜져 있으면** 등록 안 한 유사어로도 노출·과금되므로 브랜드 캠페인은 확장검색 OFF가 맞다.

진짜 낭비 구간:
1. 브랜드 키워드 과잉 입찰 — 자연검색으로도 1위인데 CPC 높게 붓는 것. 1~2위 유지할 최소 입찰만
2. 광역 키워드 — "안산아파트"류(노출 97%, CTR 0.03%)가 예산을 먹는 범인
3. 반대로 **버려지고 있는 알짜**: 검색량 20~40 롱테일(모델하우스/청약/줍줍/경쟁률/일반분양/위치). 입찰 경쟁이 없어 CPC 바닥인데 구매 의도는 최상

### 블로그 시딩 황금 키워드 (검색량 대비 발행량)
| 키워드 | 검색량 | 블로그 발행량 | 판단 |
|---|---|---|---|
| 모델하우스 | 40 | 26,500 | 포화, 진입 불가 |
| 줍줍 | 20 | **57** | 🎯 무주공산 |
| 경쟁률 | 20 | **76** | 🎯 무주공산 |
| 2차 | 20 | 240 | 노려볼 만 |
| 일반분양 | 20 | 590 | 가능 |

우리 약점이 네이버 블로그 백링크 0건이므로, 직원 5명 블로그로 이 롱테일부터 치는 게 광고비 없이 자연유입 만드는 길.
