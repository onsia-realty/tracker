{
  "buildCommand": "cd tracker && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "tracker/.next",
  "installCommand": "echo 'Skipping root install; tracker/ deps installed in buildCommand'",
  "framework": "nextjs"
}
